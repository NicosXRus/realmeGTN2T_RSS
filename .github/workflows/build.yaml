name: Build Realme GT Neo 2T Kernel (SukiSU Ultra)

on:
  workflow_dispatch:
    inputs:
      defconfig:
        description: 'Путь к defconfig (относительно arch/arm64/configs/)'
        required: true
        default: 'defconfig'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Kernel Source
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Clone Vendor Source & Setup Paths
        run: |
          echo "Загружаем вендорные модули..."
          # Клонируем во временную папку, чтобы избежать дублирования vendor/vendor
          git clone --depth=1 https://github.com/realme-kernel-opensource/realme_X7max_GTneo_GTneo2T-AndroidT-vendor-source vendor_tmp
          
          # Копируем содержимое в корень репозитория
          cp -rn vendor_tmp/* .
          rm -rf vendor_tmp

          # Автоматически находим sched_assist и делаем симлинк в kernel/
          mkdir -p kernel
          SCHED_DIR=$(find vendor/ -type d -name "sched_assist" 2>/dev/null | head -n 1)
          
          if [ -n "$SCHED_DIR" ]; then
            echo "Найдена директория sched_assist по пути: $SCHED_DIR"
            ln -sfn "../$SCHED_DIR" kernel/sched_assist
          else
            echo "sched_assist не найден в vendor, создаем пустую заглушку Kconfig..."
            mkdir -p kernel/sched_assist
            touch kernel/sched_assist/Kconfig
          fi

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            bc build-essential bison flex libssl-dev libncurses-dev \
            libelf-dev clang lld llvm ccache zip unzip git wget

      - name: Setup Toolchain (Proton Clang)
        run: |
          mkdir -p toolchain
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git toolchain/clang
          # Удаляем устаревший ld, чтобы не конфликтовал с системным glibc Ubuntu
          rm -f toolchain/clang/bin/ld

      - name: Inject SukiSU Ultra Driver & Defconfig Parameters
        run: |
          # 1. Загружаем исходники SukiSU Ultra в режиме 'builtin'
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s builtin

          # 2. Находим defconfig и добавляем параметры KSU
          DEFCONFIG_PATH="arch/arm64/configs/${{ github.event.inputs.defconfig }}"
          
          if [ ! -f "$DEFCONFIG_PATH" ]; then
            echo "::error::Файл $DEFCONFIG_PATH не найден! Проверьте имя файла."
            exit 1
          fi

          echo "" >> $DEFCONFIG_PATH
          echo "# SukiSU Ultra Flags" >> $DEFCONFIG_PATH
          echo "CONFIG_KSU=y" >> $DEFCONFIG_PATH
          echo "CONFIG_KPROBES=n" >> $DEFCONFIG_PATH
          
          echo "Параметры SukiSU внесены в $DEFCONFIG_PATH"

      - name: Build Kernel
        run: |
          export PATH="$GITHUB_WORKSPACE/toolchain/clang/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          
          mkdir -p out
          
          # Генерация .config
          make O=out ARCH=arm64 ${{ github.event.inputs.defconfig }}
          
          # Компиляция ядра
          make -j$(nproc --all) O=out \
            ARCH=arm64 \
            CC=clang \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
            CLANG_TRIPLE=aarch64-linux-gnu-

      - name: Locate Compiled Image
        run: |
          if [ -f "out/arch/arm64/boot/Image.gz-dtb" ]; then
            echo "IMAGE_PATH=out/arch/arm64/boot/Image.gz-dtb" >> $GITHUB_ENV
          elif [ -f "out/arch/arm64/boot/Image.gz" ]; then
            echo "IMAGE_PATH=out/arch/arm64/boot/Image.gz" >> $GITHUB_ENV
          elif [ -f "out/arch/arm64/boot/Image" ]; then
            echo "IMAGE_PATH=out/arch/arm64/boot/Image" >> $GITHUB_ENV
          else
            echo "::error::Скомпилированное ядро не найдено!"
            exit 1
          fi

      - name: Pack Kernel into AnyKernel3
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git AnyKernel3
          cp ${{ env.IMAGE_PATH }} AnyKernel3/
          
          cd AnyKernel3
          sed -i 's/device.name1=.*/device.name1=RMX3357/' anykernel.sh
          sed -i 's/device.name2=.*/device.name2=bitra/' anykernel.sh
          
          zip -r9 ../Kernel_SukiSU_GT_Neo_2T.zip * -x .git README.md

      - name: Upload Zip Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SukiSU-Kernel-Realme-GT-Neo-2T
          path: Kernel_SukiSU_GT_Neo_2T.zip
